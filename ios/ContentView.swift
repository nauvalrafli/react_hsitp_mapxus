//
//  ContentView.swift
//  mapxus-hsitp-ios
//
//  Created by dev01 on 05/05/25.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack(content: {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, world!")
        })
        .padding()
    }
}

#Preview {
    ContentView()
}
